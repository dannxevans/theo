name: Cleanup Old Docker Images

on:
  schedule:
    # Run weekly on Sundays at 2 AM UTC
    - cron: '0 2 * * 0'
  workflow_dispatch:  # Allow manual trigger

permissions:
  packages: write
  contents: read

env:
  REGISTRY: ghcr.io
  OWNER: dannxevans

jobs:
  cleanup:
    name: Delete Old SHA-tagged Images
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image: [theo-backend, theo-frontend]

    steps:
      - name: Delete old images for ${{ matrix.image }}
        uses: actions/delete-package-versions@v5
        continue-on-error: true  # Don't fail if package metadata becomes unavailable after cleanup
        with:
          package-name: ${{ matrix.image }}
          package-type: 'container'
          min-versions-to-keep: 10
          delete-only-untagged-versions: false
          ignore-versions: '^(latest|v.*)$'  # Keep 'latest' and version tags like 'v1.0.0'
